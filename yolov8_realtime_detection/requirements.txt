# requirements.txt

opencv-python>=4.5.0
matplotlib>=3.3.0
ultralytics>=8.0.0
numpy>=1.20.0
# Pillow is often a dependency of Matplotlib,
# explicitly adding it for clarity if needed by some backends.
Pillow>=9.0.0